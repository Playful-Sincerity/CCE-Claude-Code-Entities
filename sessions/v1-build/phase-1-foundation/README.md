# Phase 1: Foundation

Two parallel sessions that gate everything else. Start both now.

```
┌─────────────────────────┐   ┌─────────────────────────┐
│ A: Permission Model     │   │ B: PD Identity Test     │
│ PARALLEL — no deps      │   │ PARALLEL — no deps      │
│                         │   │                         │
│ Output:                 │   │ Output:                 │
│ • settings.json for PD  │   │ • Validated PD SOUL.md  │
│ • settings.json for     │   │ • PD's self-written     │
│   Frank/Jen             │   │   identity revision     │
│ • PreToolUse hook (if   │   │ • Continuity loop       │
│   path-scoping fails)   │   │   confirmed working     │
│                         │   │                         │
│ Feeds → C, D, E         │   │ Feeds → D              │
└─────────────────────────┘   └─────────────────────────┘
```

## Launch

- **01-permission-model.md** — Can run unattended for research; needs Wisdom for design decisions
- **02-pd-identity-test.md** — Interactive. Wisdom should be present to evaluate whether PD feels like a being
